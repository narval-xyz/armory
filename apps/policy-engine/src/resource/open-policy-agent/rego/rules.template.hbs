package main import data.armory.criteria

{{#each policies}}
  {{then}}[{"policyId": "{{id}}", "policyName": "{{name}}" }] = reason {
  {{#each when}}
    {{#criterion this}}{{/criterion}}
  {{/each}}
  {{#reason this}}{{/reason}}
  }
{{/each}}